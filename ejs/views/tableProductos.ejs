<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container col-sm-6">         
        <% if(hayProductos) { %> 
            <table class="table">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Precio</th>
                    <th>Imagen</th>
                </tr>
                <tbody>
                <% prods.map(producto => { %> 
                    <tr>
                        <td><%= producto.id %></td>
                        <td><%= producto.title %></td>
                        <td><%= producto.price %></td>
                        <td><img src=<%= producto.thumbnail %> alt="no found"> </td>
                    </tr>
                <% }) %> 
            </tbody>
            </table>
        <% } else { %> 
            <p>No hay productos</p>
        <% } %> 
        <br>
        <a href="/" class="btn btn-primary" type="button">Formulario Producto</a>
    </div>
</body>